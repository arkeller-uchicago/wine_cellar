<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h1>
      Wine #<%= @wine.id %> details
    </h1>

    <div class="row mb-3">
      <div class="col">
        <a href="/wines" class="btn btn-block btn-outline-secondary">
          Go back
        </a>
      </div>

      <div class="col">
        <a href="/wines/<%= @wine.id %>/edit" class="btn btn-block btn-outline-secondary">
          Edit wine
        </a>
      </div>

      <div class="col">
        <a href="/delete_wine/<%= @wine.id %>" class="btn btn-block btn-outline-secondary">
          Delete wine
        </a>
      </div>
    </div>

    <dl>
      <dt>
        Name
      </dt>
      <dd>
        <%= @wine.name %>
      </dd>

      <dt>
        Type
      </dt>
      <dd>
        <% if @wine.type.present? %>
          <a href="/types/<%= @wine.type_id %>">
            <%= @wine.type.name %>
          </a>
        <% end %>
      </dd>

      <dt>
        Coutry
      </dt>
      <dd>
        <% if @wine.coutry.present? %>
          <a href="/countries/<%= @wine.coutry_id %>">
            <%= @wine.coutry.name %>
          </a>
        <% end %>
      </dd>

      <dt>
        Region
      </dt>
      <dd>
        <% if @wine.region.present? %>
          <a href="/regions/<%= @wine.region_id %>">
            <%= @wine.region.name %>
          </a>
        <% end %>
      </dd>

      <dt>
        Producer
      </dt>
      <dd>
        <% if @wine.producer.present? %>
          <a href="/producers/<%= @wine.producer_id %>">
            <%= @wine.producer.name %>
          </a>
        <% end %>
      </dd>

      <dt>
        Created at
      </dt>
      <dd>
        <%= time_ago_in_words(@wine.created_at) %> ago
      </dd>

      <dt>
        Updated at
      </dt>
      <dd>
        <%= time_ago_in_words(@wine.updated_at) %> ago
      </dd>
    </dl>
  </div>
</div>

<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <%= render partial: "wine_templates/wine_cellars" %>
  </div>
</div>

<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <%= render partial: "wine_templates/wine_ratings" %>
  </div>
</div>
