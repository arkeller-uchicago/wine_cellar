<div class="card">
  <div class="card-header d-flex align-items-center justify-content-between">
    Cellars

    <a data-toggle="collapse" href="#wine_cellars_new_form">
      Add cellar
    </a>
  </div>

  <ul class="list-group list-group-flush">
    <% @wine.cellars.each do |cellar| %>
      <li class="list-group-item d-flex align-items-center justify-content-between">
        <!-- Show link start -->
        <a href="/cellars/<%= cellar.id %>">
          <%= cellar.user_id %>
        </a>
        <!-- Show link end -->

        <!-- Delete link start -->
        <%if current_user == cellar.user %>
          <a href="/delete_cellar_from_wine/<%= cellar.id %>">
            Delete
          </a>
        <% end %>
        <!-- Delete link end -->

      </li>
    <% end %>

    <div class="card-footer">
      <div class="collapse" id="wine_cellars_new_form">
        <!-- New associationcollection form start -->
        <%= render partial: "wine_templates/wine_cellars_new_form" %>
        <!-- New associationcollection form end -->
      </div>
    </div>

  </ul>
</div>
