<form action="/create_wine" method="post" enctype="multipart/form-data">

  <div class="form-group">
    <label for="name">
      Name
    </label>

    <input type="text" id="name" name="name" class="form-control" value="<%= @wine.name %>">
  </div>

  <input type="hidden" name="bottle_size_id" value="<%= @type.id %>">

  <div class="form-group">
    <label for="type_id">
      Type
    </label>

    <%= select_tag(:type_id, options_from_collection_for_select(Country.all, :id, :name, @wine.type_id), :class => "form-control") %>
  </div>

  <div class="form-group">
    <label for="coutry_id">
      Coutry
    </label>

    <%= select_tag(:coutry_id, options_from_collection_for_select(Region.all, :id, :name, @wine.coutry_id), :class => "form-control") %>
  </div>

  <div class="form-group">
    <label for="region_id">
      Region
    </label>

    <%= select_tag(:region_id, options_from_collection_for_select(Producer.all, :id, :name, @wine.region_id), :class => "form-control") %>
  </div>

  <div class="form-group">
    <label for="producer_id">
      Producer
    </label>

    <input type="text" id="producer_id" name="producer_id" class="form-control" value="<%= @wine.producer_id %>">
  </div>

  <div class="form-group">
    <label for="picture">
      Picture
    </label>

    <input type="file" id="picture" name="picture" class="form-control">
  </div>

  <button class="btn btn-block btn-outline-secondary">
    Create wine
  </button>
</form>
